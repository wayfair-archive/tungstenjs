# Tungsten.js + SVG Example

Example application showing how Tungsten.js can be used with SVG.

## Run

    # Run `npm install` on the main tungstenjs directory
    npm install
    npm start
    # To run in debug mode: `npm start -- --dev`